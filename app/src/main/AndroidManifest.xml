<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thmlocals3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.THMLocals3">
        <activity android:name=".Six"></activity>
        <activity android:name=".Five" />
        <activity android:name=".Four" />
        <activity android:name=".Three" />
        <activity android:name=".Two" />
        <activity android:name=".One" />
        <activity android:name=".Kennenlernen" />
        <activity android:name=".Gruppen" />
        <activity android:name=".Nachhilfe" />
        <activity android:name=".Module" />
        <activity android:name=".Dashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Chat_Room" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>